package zones;

import zones.Zone;

public class BicyclePath extends Zone {

    public BicyclePath(int l, int w, String t, boolean p, int sx, int sy, int ex, int ey) {
        super(l, w, t, p, sx, sy, ex, ey);
    }
}
